/**
 * Firebase integration stub (TDD §1, §4).
 * Wire Auth (Google / Apple / Facebook), Firestore, Cloud Functions, Analytics here.
 *
 * Example:
 * import { initializeApp } from 'firebase/app'
 * const app = initializeApp({
 *   apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
 *   ...
 * })
 */
export const firebaseConfigPlaceholder = {
  projectId: 'your-predictx-project',
} as const
